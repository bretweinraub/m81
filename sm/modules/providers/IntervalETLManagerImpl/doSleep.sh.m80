<: # -*-sh-*-
use Helpers::shellHelpers; # $m80path = [{command => "embedperl"}, {command => "m4", chmod => "+x" }];
print Helpers::shellHelpers::shellScript ( r => [sleepInterval],
                                           getopts => ''); 
:>

sleep $sleepInterval

cleanup 0

    
